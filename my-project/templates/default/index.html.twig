{% extends 'base.html.twig' %}

{% block title %}
	Hello DefaultController!

	{% set string = 'some string \n from \n database' %}
	{% set users = [{name: 'John'},{name: 'Susan'},{name: 'Robert'}] %}

{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Hello
			{{ controller_name | upper}}! ✅</h1>

		This friendly message is coming from:
		<ul>
			<li>Your controller at
				<code>
					<a href="{{ 'F:/xampp/htdocs/Symfony Web Development/my-project/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a>
				</code>
			</li>
			<li>Your template at
				<code>
					<a href="{{ 'F:/xampp/htdocs/Symfony Web Development/my-project/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a>
				</code>
			</li>
		</ul>
		

	{{ app.user }}
	{{ app.request.get('param') }}
	{{ app.environment }}
	{# {{ dump(app.session) }} #}
	
		<ul>
			{% for user in users %}
				{% if random_gift[loop.index0] is defined %}
			
					<li>{{ user.name }}
						- you won
						{{ random_gift[loop.index0] }}</li>
					{% endif %}
			{% endfor %}
		</ul>

		{# {% for label, messages in app.flashes %}
			{% for message in messages %}`
				<div class="flash-{{ label }}">
					{{ message }}
				</div>
			{% endfor %}
		{% endfor %} #}


		{% for user in users %}
			{% if user.name == 'Susan' %}

				<p>
					{{ user.name }}
				</p>
			{% else %}
				No users where found
			{% endif %}
		{% endfor %}

		{% include "includes/subview.html.twig" with {'users': users} %}

		{% for i in 1..10 %}
			<div class="{{ cycle(['even', 'odd'], i) }}">
				{{ i }}
			</div>
		{% endfor %}

		{# {{ string|escape('html') }}<br> #}
		{# {{ string|escape('css') }}<br> #}
		{{ path('home') }}<br>
		{{ url('home') }}<br>
		{{ asset('images/logo.png') }}<br>
		{{ absolute_url(asset('images/logo.png')) }}<br>
		The google tracking code is: {{ ga_code }}<br>




		{{ 
			render(
				controller(
					'App\\Controller\\DefaultController::mostPopularPosts',
					{ number: 5 }
				)
			) 
		}}


	</div>

{% endblock %}


{% block javascripts %}
	{{ parent() }}
	<script>
		{# alert(2)#}
	</script>
	<script>
		{# var route = "{{ string|escape('js') }}"; #}
	</script>
{% endblock %}
